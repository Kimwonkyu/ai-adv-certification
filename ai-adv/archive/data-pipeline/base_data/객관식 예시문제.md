---
1. RAG 시스템의 최종 목표로 가장 적절한 것은 무엇입니까?
   1. 결과의 정확도보다는 가장 빠른 검색 속도를 달성
   2. 가장 작은 벡터 데이터베이스 구축
   3. 효과적인 컨텍스트 구성과 검색 효율 향상을 통해 정확한 답변을 생성하는 것
   4. 가장 많은 수의 무서를 검색하는 것
   5. LLM을 사용하지 않고 검색만으로 답변을 제공하는 것

2. RAG 파이프라인의 'Processing'단계에서 수행하는 작업으로 가장 적절한 것은 무엇입니까?
   1. 원본 문서를 청크로 분할한다.
   2. 사용자의 질문을 검색에 더 적합하도록 명확하게 만들거나 확장한다.
   3. 검색된 청크를 LLM 프롬프트에 삽입한다.
   4. 최종 답변을 사용자에게 보여준다.
   5. 텍스트를 임베딩 벡터로 변환한다.

3. SFT(SupervisedFine-Tuning)에 대한 설명으로 가장 적절한 것은?
   1. 언어모델을 처음부터 학습하는 단계로 Pretraining과 같은 개념이다.
   2. 모델의 파라미터를 업데이트하지 않고, 프롬프트만을 바꾸어 사용하는 방법을 의미한다.
   3. 사람이 만든 정답이 포함된 데이터로, 이미 만들어진 언어 모델을 특정 작업에 맞게 다시 학습시키는 단계이다.
   4. SFT는 항상 강화학습(RL)과 함께 사용되어아먄 한다.
   5. SFT는 추론(reasoning)성능 향상에는 사용할 수 없고, 단순 분류(classification)과제에 한해 사용할 수 있다.

4. 파이썬 프로젝트에서 가상 환경(virtual enviroment)을 사용하는 주된 이유로 가장 적절한 것은 무엇입니까?
   1. 코드 실행 속도를 높이기 위해
   2. 프로젝트별로 독립된 라이브러리 버전을 관리하기 위해
   3. 파이썬 코드를 다른 언어로 자동 변환하기 위해
   4. 여러 컴퓨터에서 코드를 동시에 실행하기 위해
   5. 데이터베이스 연결을 암호화하기 위해

5. 트랜스포머 모델이 단어의 순서 정보를 학습할 수 있도록 입력 임베딩에 더해주는 정보는 무엇입니까?
   1. 토큰 타입 임베딩 (Token Type Embedding)
   2. 포지셔널 인코딩 (Positional Encoding)
   3. 어텐셜 마스크 (Attention Mask)
   4. 레이어 정규화 (Layer Normalization)
   5. 피드포워드 네트워크 (Feed-Forward Network)

6. 다음 Numpy코드의 실행 결과로 가장 알맞은 것은 무엇입니까?

```python
import numpy as np
a = np.array([1,2,3])
b = a + 10
print(b)
```
1. [1,2,3,10]
2. 오류가 발생한다.
3. [11,12,13]
4. [1,2,3]
5. [array([1,2,3]),10]

7. Pandas를 사용하여 현재 작업 디렉토리에 있는 'data.csv'파일을 데이터프레임 df로 읽어오려고 합니다. 가장 적절한 코드는 무엇입니까?
   1. df=pd.read_text("data.csv")
   2. df=pd.read_csv("data.csv")
   3. df=pd.DataFrame("data.csv")
   4. df=pd.open("data.csf")
   5. df=pd.load_csv("data.csv")
   
8. LLM이 웹검색 API를 호출하거나 계산기를 사용하는 등 외부의 함수나 API를 사용할 수 있게 하는 기능은 무엇입니까?
   1. RAG(Retrieval Augmented Generation)
   2. 파인튜닝(Fine-tuning)
   3. 임베딩(Embedding)
   4. 도구 호출(Tool Calling)
   5. 사전 학습(Pre-training)

9. LLM 응답을 JSON 형식의 구조화된 출력으로 받으려 할 때, 프롬프트 엔지니어링 관점에서 가장 적절한 설계 원칙은?
   1.  출력 형식에 제한을 두지 않고 "자유롭게 설명해 달라"고 요청한다.
   2.  모델이 JSON 형식을 정확히 따르지 않을 것이라고 가정하고, 구조화 요구 없이 자연어만 갈게 받은 뒤, 후처리 한다.
   3.  원하는 필드 이름과 자료형, 허용 값 범위 등을 스키마 형태로 제시하고, 이 스키마에 맞는 JSON만 응답하도록 지시한다.
   4.  실제 JSON 예시를 보여주기 보다는 구조를 대략 설명해 주고 응답 하도록 지시해준다.
   5.  파싱 오류를 줄이기 위해, 모델이 응답 마지막 줄에만 JSON을 쓰게 하고 앞부분에는 자유 형식의 설명을 섞어 쓰도록 한다.

10. 청킹시, 문장 시작 부분이 잘려 문맥이 손실되는 것을 방지하고자 청크의 앞부분에 상위 레벨의 요약이나 제목을 추가하는 기법은 무엇입니까?
    1.  Contextual Retrieval
    2.  Summary-Prepended Chunking(Small2Big Chunking)
    3.  Reordering
    4.  HyDE
    5.  Rank Fusion