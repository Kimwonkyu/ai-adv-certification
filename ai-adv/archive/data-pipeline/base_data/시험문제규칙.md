***

- 시험은 각 단원별로 **객관식 100문제와 코드 완성형 10문제**, 총 110문제로 구성됩니다.
- AI 개발에 필수적인 Python, LLM, 데이터 전처리, 프롬프트 엔지니어링, RAG/Agent, Fine tuning의 기본 개념 및 활용 방법에 대한 내용이 출제됩니다.
- **시험 통과 기준: 총점 80점 이상**
- 문제 배점: 객관식은 난이도에 따라 배점되며, 코드 완성형은 별도의 배점 기준을 따릅니다.

## 객관식 보기 구성 규칙
- 보기는 반드시 5개(5지선다)로 구성되어야 합니다.
- **"맞는게 없음", "정답 없음", "모두 정답"과 같은 형식의 보기는 절대 사용하지 않습니다.**
- 반드시 5개의 보기 중에 명확한 정답이 1개 이상 존재해야 합니다.

## 예시문제
- 객관식
  Q. RAG 시스템의 최종 목표로 가장 적절한 것은 무엇입니까?(4점)
     1.결과의 정확도보다는 빠른 검색 속도를 달성
     2.가장 작은 벡터 데이터베이스 구축
     3.효과적인 컨텍스트 구성과 검색 효율 향상을 통해 정확한 답변을 생성하는 것
     4.가장 많은 수의 문서를 검색하는것
     5.LLM을 사용하지 않고 검색만으로 답변을 제공하는 것

- 코드 완성형
  코드 완성형 문제는 아래와 같은 화면처럼 구성되며,
  "#이 부분을 채우세요" 라고 표기된 빈 칸에 코드를 완성하여 제출하시면 됩니다.

  # 빈칸을 채워 MultiQueryRetriver를 설정하세요

```python
# MultiQueryRetriever 생성
multi_query_retriever = MultiQueryRetriever._____(# 이 부분을 채우세요
   ______=base_retriever, # 이 부분을 채우세요
   ______=rewrite_prompt, # 이 부분을 채우세요
   ______=llm             # 이 부분을 채우세요
)

print("MultiQueryRetriever 생성 완료!")
print(f"타입: {type(multi_query_retriever)}")

# 검색 테스트
test_queries = [
    "웹 개발 기술",
    "데이터 저장 방법",
    "배포 도구"
]

print("\n=== MultiQueryRetriever 테스트 ===")
results = {}

for i, query in enumerate(test_queries, 1):
    print(f"\n{i}. 쿼리: {query}")

    # MultiQuery 검색
    multi_results = multi_query_retriever.invoke(query)
    results[query] = multi_results

    print(f"   검색 결과 수: {len(multi_results)}")

    for j, doc in enumerate(multi_results, 1):
        category = doc.metadata.get('category','Unknown')
        print(f"   {j}, [{category}] {doc.page_content}")

# 결과 요약
total_results = sum(len(docs) for docs in results.values())
unique_docs = set()
for docs in results.values():
    for doc in docs:
        unique_docs.add(doc.page_content)

print(f"\n=== 결과 요약 ===")
print(f"총 검색 결과: {total_result}개")
print(f"고유 문서: {len(unique_docs)}개")
print(f"평균 결과 수: {total_results / len(test_queries):.if}개")